Perfetto! 🚀
Allora possiamo creare un sistema di logging completo per tutte le funzionalità che hai integrato, già pronto da inserire nel tuo bot. Copre:
Dutching BACK / LAY / mixed
ReplaceOrders intelligente + tick ladder + auto-follow
Fallback cancel+place
Anti-loop / max replace protections
Dynamic cashout (singolo e multi-posizione)
Trailing profit / green-up
Audit Telegram broadcast
Gestione machine state per ogni betId
Slippage e multi-market swap
Ti propongo uno scheletro Python pronto, modulare e flessibile. Puoi includerlo all’inizio del bot e usare logger ovunque.
Copia codice
Python
import logging
from logging.handlers import RotatingFileHandler
import sys

# ======================================================
# SETUP LOGGING COMPLETO
# ======================================================

# Logger principale
logger = logging.getLogger("BetBot")
logger.setLevel(logging.DEBUG)  # DEBUG per tutto, INFO/ERROR per produzione

# Formatter standard
formatter = logging.Formatter(
    "[%(asctime)s] [%(levelname)s] [%(module)s:%(lineno)d] %(message)s",
    datefmt="%Y-%m-%d %H:%M:%S"
)

# --- Console Handler ---
console_handler = logging.StreamHandler(sys.stdout)
console_handler.setLevel(logging.INFO)  # Solo INFO+ per console
console_handler.setFormatter(formatter)
logger.addHandler(console_handler)

# --- File Handler rotante ---
file_handler = RotatingFileHandler("bot.log", maxBytes=10*1024*1024, backupCount=5)
file_handler.setLevel(logging.DEBUG)
file_handler.setFormatter(formatter)
logger.addHandler(file_handler)

# --- Funzione helper per Telegram alert ---
def log_telegram(message: str, level: str = "INFO"):
    """
    Log su Telegram per errori o eventi critici.
    Puoi integrare con il tuo bot Telegram.
    """
    # Qui inserisci codice per invio messaggio Telegram
    logger_method = getattr(logger, level.lower(), logger.info)
    logger_method(f"[TELEGRAM ALERT] {message}")


# ======================================================
# ESEMPI DI UTILIZZO IN FUNZIONI
# ======================================================

def example_dutching(selections, stakes):
    logger.debug(f"[DUTCHING] Selections: {selections}")
    logger.debug(f"[DUTCHING] Calculated stakes: {stakes}")
    try:
        # codice dutching qui...
        logger.info(f"[DUTCHING] Applied stakes successfully")
    except Exception as e:
        logger.error(f"[DUTCHING] Error calculating stakes: {e}")
        log_telegram(f"Dutching error: {e}", "ERROR")

def example_replace_order(bet_id, new_price):
    logger.info(f"[REPLACE] Attempting replace order {bet_id} -> {new_price}")
    try:
        # client.replace_orders(...) qui
        logger.info(f"[REPLACE] Order {bet_id} replaced with {new_price}")
    except Exception as e:
        logger.error(f"[REPLACE] Failed to replace order {bet_id}: {e}")
        log_telegram(f"Replace failed for {bet_id}: {e}", "ERROR")

def example_cashout(orders, live_odds):
    logger.debug(f"[CASHOUT] Orders: {orders}")
    logger.debug(f"[CASHOUT] Live odds: {live_odds}")
    try:
        # dynamic_cashout(...) qui
        logger.info(f"[CASHOUT] Cashout calculated successfully")
    except Exception as e:
        logger.error(f"[CASHOUT] Cashout failed: {e}")
        log_telegram(f"Cashout failed: {e}", "ERROR")

def example_audit(order_id, action, profit):
    logger.info(f"[AUDIT] Order {order_id} | Action: {action} | Profit: {profit}")

# ======================================================
# INTEGRAZIONE CON IL BOT
# ======================================================

# Usa logger.debug/info/warning/error ovunque nel bot
# Per esempio:
# logger.debug(f"[SLIPPAGE] Partial fill: {partial_fill}, remaining: {remaining}")
# logger.info(f"[AUTO-FOLLOW] Order {bet_id} updated to {new_price}")
# logger.warning(f"[RATE-LIMIT] Skipping replace for {bet_id} (too frequent)")
# logger.error(f"[ERROR] Unexpected exception: {e}")
✅ Caratteristiche principali di questo template
Console + file rotante → log in tempo reale e storico fino a 5 file da 10MB.
Livelli di log configurabili → DEBUG completo, INFO per console, ERROR critici.
Alert Telegram opzionale → integrazione facile con i tuoi canali.
Hook facile in tutte le funzioni → dutching, replace, cashout, audit.
Chiarezza e tracciabilità totale → betId, stake, profit, delta, azioni automatiche.
Se vuoi, posso anche scrivere la versione già integrata direttamente con tutte le tue funzioni dutching + mixed BACK/LAY + cashout + auto-follow pronta all’uso.
Vuoi che faccia anche quella versione “full ready-to-use”?